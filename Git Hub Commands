To clone a repo :
  git clone 'url'
To push to a branch do these :
  git checkout main
  git pull origin main
  git checkout -b feature-branch
  git add .
  git commit -m "Your commit message here"
  git push origin feature-branch:Dev-BackEnd
To Pull changes into your local repo :
  git pull origin Dev-BackEnd
